package net.thumbtack.school.notes.enums;

/**
 * Types of sorting the returned list of user accounts after user request
 */
public enum ParamSort {
    /**
     * Sort in ascending order
     */
    ASC,
    /**
     * Sort in descending order
     */
    DESC
}